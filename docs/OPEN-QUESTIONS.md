# Open Questions

## JunFlow CLI - 2026-03-08 (2차 개정 2026-03-09)

- [x] ~~Notion 데이터베이스 속성 매핑: Jun님의 실제 Notion 이슈 DB가 어떤 속성(status, priority, labels 등)을 사용하는지 확인 필요~~ -- **[개정 2차] Task 0으로 승격. 경험정리 DB(`311739cc-9a3f-8140-a758-e4c8c7145b49`)는 확인 완료. 이슈 트래커용 DB 선정 및 스키마 매핑은 Task 0에서 실행 시점에 확정.**
- [ ] npm 패키지명 `junflow` 사용 가능 여부 확인 필요 -- npm registry에 이미 존재할 수 있음
- [ ] Claude 모델 선택: 커밋 메시지/리뷰 등 각 에이전트별로 다른 모델(Haiku vs Sonnet)을 사용할지 단일 모델로 갈지 -- 비용 최적화 vs 구현 단순성
- [ ] 브랜치 네이밍 컨벤션에서 한글 이슈 제목 처리 방식 결정 필요 -- 영문 변환(AI), slug화, 또는 이슈 번호만 사용
- [ ] v2 확장 시 Jira 어댑터의 인증 방식(OAuth vs API Token) 결정 -- 현재는 Notion만 지원하므로 즉시 결정 불필요

## JunFlow CLI - 2차 개정에서 추가된 질문 (2026-03-09)

- [ ] 이슈 트래커용 Notion DB: 기존 경험정리 DB를 활용할지, 별도 이슈 전용 DB를 생성할지 -- Task 0 실행 시 Jun님 결정 필요
- [ ] AI 응답 파싱 재시도 시 비용: 재시도 1회가 API 비용에 미치는 영향 허용 가능 범위 -- 실사용 시 빈도에 따라 결정
